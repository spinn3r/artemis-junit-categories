package com.spinn3r.junit.categories;

/**
 * This test generates on disk corpora that need to change.  This is done so
 * that when we change code that writes corpora, that we can quickly run just
 * tests that change the corpora.
 */
public class Corpora {
}
